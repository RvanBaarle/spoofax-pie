/**
 * To evaluate a test case, we need its test fragment (the input source code, its language, etc)
 * and we invoke each expectation on it. Expectations may parse the input, analyze the input, or invoke a strategy on it.
 * Each expectation produces a result.
 */
package mb.spt.evaluation;
